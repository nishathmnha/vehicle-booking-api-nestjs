PORT=3000
NODE_ENV=development
APP_HOST=0.0.0.0

# Optional for cloud deployments. If set, DATABASE_URL takes precedence over the DB_* values below.
# DATABASE_URL=postgresql://user:password@host:5432/database

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=vehicle_booking
DB_SYNCHRONIZE=true
DB_SSL=false
